"use client";

import { LoadingOverlay } from "@/components/ui/loading";

const SignOutOverlay = () => {
  return <LoadingOverlay title="Signing out..." text="Please wait" />;
};

export default SignOutOverlay;
